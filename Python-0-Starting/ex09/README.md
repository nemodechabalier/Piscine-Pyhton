# ft_package# ft_package



A tiny sample package for the Piscine Python.A small example Python package.



## What it provides## Usage



- `count_in_list(items, value)` → counts how many times `value` appears in `items`.```python

from ft_package import count_in_list

## Example

print(count_in_list(["toto", "tata", "toto"], "toto"))  # 2

```pythonprint(count_in_list(["toto", "tata", "toto"], "tutu"))  # 0

from ft_package import count_in_list

print(count_in_list(["toto", "tata", "toto"], "toto"))
print(count_in_list(["toto", "tata", "toto"], "tutu"))
```
