def count_in_list(lst: list, value) -> int:
    """
    Count how many times 'value' appears in the list 'lst'.

    Parameters:
    - lst: list of elements
    - value: element to count

    Returns:
    - int: number of occurrences of value in lst
    """
    return lst.count(value)
